# Dependencies
node_modules/

# Build output
dist/

# Playwright run artifacts
test-results/
playwright-report/

# Package tarballs
*.tgz

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Editor directories and files
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# OS files
Thumbs.db

# Environment variables
.env
.env.local
.env.*.local

# TypeScript
*.tsbuildinfo

# Generated custom-elements-manifest + editor-integration artifacts.
# Regenerated by `npm run analyze` / `npm run build` and shipped via the
# package.json "files" field — kept out of git for the same reason as dist/
# (build output, derived from ATTRIBUTE_TABLE + MultiSelectConfig JSDoc).
custom-elements.json
web-types.json
vscode.html-custom-data.json
# Emitted empty (theming vars live in component-variables.manifest.json); not shipped.
vscode.css-custom-data.json
